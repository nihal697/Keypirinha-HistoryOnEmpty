#
# HistoryOnEmpty Package configuration file
# Recent history as a keyword: type "h", Tab -> recent launches
#

[main]
# Maximum number of history items to suggest (range 1-100)
#max_items = 8

# Explicit path to Keypirinha.history (empty = auto-detect:
# live dev layout first, then %APPDATA% / %LOCALAPPDATA%)
#history_path =


[var]


[env]
